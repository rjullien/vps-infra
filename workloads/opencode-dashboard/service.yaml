apiVersion: v1
kind: Service
metadata:
  name: opencode-dashboard
  labels:
    app: opencode-dashboard
spec:
  selector:
    app: opencode-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
