apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opencode-dashboard-cert
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "opencode.bapttf.com"
