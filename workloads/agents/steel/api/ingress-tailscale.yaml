apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: steel-api-ingress
  namespace: openclaw
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - steel-api
  defaultBackend:
    service:
      name: steel-api
      port:
        number: 3000