apiVersion: v1
kind: Service
metadata:
  name: kirocrew
  namespace: openclaw
  labels:
    app: kirocrew
spec:
  selector:
    app: kirocrew
  ports:
    - protocol: TCP
      port: 5476
      targetPort: dashboard
      name: dashboard
