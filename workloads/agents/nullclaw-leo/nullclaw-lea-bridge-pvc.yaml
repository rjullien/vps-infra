apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nullclaw-lea-bridge
  namespace: openclaw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-path
