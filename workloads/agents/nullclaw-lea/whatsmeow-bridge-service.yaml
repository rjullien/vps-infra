apiVersion: v1
kind: Service
metadata:
  name: whatsmeow-bridge-service
  namespace: openclaw
  labels:
    app: whatsmeow-bridge
spec:
  selector:
    app: whatsmeow-bridge
  ports:
    - protocol: TCP
      port: 3301
      targetPort: http
      name: http
