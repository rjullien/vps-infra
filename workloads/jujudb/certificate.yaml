apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jujudb-cert
  namespace: jujudb
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "jujudb.bapttf.com"
