apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: litellm-infisical-secret
  namespace: openclaw
  annotations:
    argocd.argoproj.io/sync-wave: "-1" 
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: infrastructure # Adjust if OpenClaw is in a different project
        envSlug: prod
        secretsPath: /openclaw/litellm
      credentialsRef:
        secretName: infisical-universal-auth
        secretNamespace: infisical
  managedKubeSecretReferences:
    - secretName: litellm-secret # This is the secret your Deployment will use
      secretNamespace: openclaw
      secretType: Opaque
      creationPolicy: Owner