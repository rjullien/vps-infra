apiVersion: v1
kind: Service
metadata:
  name: openclaw-ssh
spec:
  publishNotReadyAddresses: true
  selector:
    app: openclaw
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22
      name: ssh