apiVersion: v1
kind: Service
metadata:
  name: openclaw-ssh
spec:
  type: LoadBalancer
  selector:
    app: openclaw
  ports:
    - protocol: TCP
      port: 22222
      targetPort: 22
      name: ssh