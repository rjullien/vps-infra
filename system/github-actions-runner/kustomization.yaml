# Base resources for ARC (namespaces + secrets)
# The Helm charts are deployed via separate ArgoCD Applications
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - infisical-secret.yaml
