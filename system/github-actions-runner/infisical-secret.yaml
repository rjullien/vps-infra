# Infisical → Kubernetes secret for GitHub PAT
# ARC expects key 'github_token' (lowercase) in the secret
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: github-runner-secrets-sync
  namespace: arc-runners
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 300
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: infrastructure
        envSlug: prod
        secretsPath: /github-actions-runner
      credentialsRef:
        secretName: infisical-universal-auth
        secretNamespace: infisical
  managedKubeSecretReferences:
    - secretName: github-runner-pat
      secretNamespace: arc-runners
      secretType: Opaque
      creationPolicy: Owner
