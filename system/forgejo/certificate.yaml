apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo-cert
  namespace: forgejo
spec:
  secretName: tls-certificate 
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "git.bapttf.com"