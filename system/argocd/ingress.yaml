# system/argocd/ingress.yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server-ingress
  namespace: argocd
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`argocd.192.168.122.133.nip.io`)
    kind: Rule
    services:
    - name: argocd-server
      port: 80