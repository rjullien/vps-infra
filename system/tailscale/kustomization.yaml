apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailscale

helmCharts:
  - name: tailscale-operator
    repo: https://pkgs.tailscale.com/helmcharts
    version: 1.0.0
    releaseName: tailscale
    namespace: tailscale
    valuesFile: values.yaml

resources:
  - 01-tailscale-infisical-secret.yaml
