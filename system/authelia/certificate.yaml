apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authelia-cert
  namespace: authelia
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "auth.juju.bapttf.com"
