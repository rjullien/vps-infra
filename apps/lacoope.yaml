apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lacoope
  namespace: argocd
  annotations:
      argocd.argoproj.io/manifest-generate-paths: .
spec:
  project: default
  source:
    repoURL: 'https://github.com/BaptTF/vps-infra.git'
    targetRevision: HEAD
    path: workloads/lacoope
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: lacoope
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
