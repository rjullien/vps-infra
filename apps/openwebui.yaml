apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openwebui-workload
  namespace: argocd
  annotations:
    argocd.argoproj.io/manifest-generate-paths: .
spec:
  project: default
  source:
    repoURL: 'https://github.com/BaptTF/vps-infra.git'
    targetRevision: HEAD
    path: workloads/openwebui
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: openwebui
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
