apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voyage
  namespace: argocd
  annotations:
      argocd.argoproj.io/manifest-generate-paths: .
spec:
  project: default
  source:
    repoURL: 'https://github.com/BaptTF/vps-infra.git'
    targetRevision: HEAD
    path: workloads/voyage
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: voyage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
